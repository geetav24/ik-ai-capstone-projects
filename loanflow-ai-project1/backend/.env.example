# Use mock first so the project runs even without a local LLM.
# Switch to ollama after you confirm backend + UI work.
LLM_PROVIDER=mock
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=gemma2:2b
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
